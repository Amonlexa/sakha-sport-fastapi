services:
  api:
    build: .
    container_name: sakha_sport_api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
